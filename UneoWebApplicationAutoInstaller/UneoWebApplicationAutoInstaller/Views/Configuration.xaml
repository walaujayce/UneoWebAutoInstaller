<Page x:Class="UneoWebApplicationAutoInstaller.Views.Configuration"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:UneoWebApplicationAutoInstaller.Views"
      xmlns:vm="clr-namespace:UneoWebApplicationAutoInstaller.ViewModels" d:DataContext="{d:DesignInstance Type=vm:ConfigurationViewModel}"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Configuration">

    <Page.Resources>
        <Style x:Key="ButtonBorderStyle" TargetType="Border">
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Width" Value="150"/>
            <Setter Property="CornerRadius" Value="22.5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background">
                <Setter.Value>
                    <SolidColorBrush Color="#0078D3"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation
                Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                To="#0C49C2"
                Duration="0:0:0.1"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation
                Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                To="#0078D3"
                Duration="0:0:0.1"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>                
            </Style.Triggers>
        </Style>
        
        <Style x:Key="ButtonTextBlockStyle" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontFamily" Value="tahoma"/>
            <Setter Property="Foreground" Value="white"/>
        </Style>
                
        <Style x:Key="CheckboxBorderStyle" TargetType="Border">
            <Setter Property="Margin" Value="0,0,0,0"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background">
                <Setter.Value>
                    <SolidColorBrush Color="Transparent"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation
            Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
            To="Transparent"
            Duration="0:0:0.1"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <ColorAnimation
            Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
            To="Transparent"
            Duration="0:0:0.1"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CheckButtonStyle" TargetType="CheckBox">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Border Style="{DynamicResource CheckboxBorderStyle}">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0">
                                <!-- Rectangle -->
                                <Grid>
                                    <Rectangle 
                                        x:Name="Rectangle"
                                        Margin="-5,0,0,0"
                                        Width="15"
                                        Height="15"
                                        Stroke="Black"
                                        StrokeThickness="0.5"
                                        Fill="Transparent"
                                        VerticalAlignment="Center"/>
                                    <Border Height="20" Width="20" Margin="0,0,0,3" BorderBrush="Transparent" BorderThickness="1.5" Panel.ZIndex="10" VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <Image Source="{Binding CheckboxImage}"/>
                                    </Border>

                                </Grid>
                                <!-- Space between circle and text -->
                                <ContentPresenter Margin="8,0,0,0"
                                        VerticalAlignment="Center"
                                        RecognizesAccessKey="True"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Optional: MouseOver effect -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Rectangle" Property="StrokeThickness" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox" x:Key="stlComboBox">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid Background="#F7FDF7">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="0.7*"/>
                                <ColumnDefinition Width="0.3*" MaxWidth="30"/>
                            </Grid.ColumnDefinitions>
                            <TextBox  Grid.Column="0" IsReadOnly="True" Text="{TemplateBinding Text}"></TextBox>
                            <Border  Grid.Column="0" BorderThickness="1,1,0,1" BorderBrush="#81D779" CornerRadius="1,0,0,1">
                            </Border>
                            
                            <Border Grid.Column="1" BorderThickness="0,1,1,1" BorderBrush="#81D779" CornerRadius="0,1,1,0">
                            </Border>
                           
                            <Popup IsOpen="True" Placement="Bottom" x:Name="Popup" Focusable="False" AllowsTransparency="True" PopupAnimation="Slide">
                                <Border CornerRadius="1" MaxHeight="{TemplateBinding MaxDropDownHeight}" MinWidth="{TemplateBinding ActualWidth}" x:Name="DropDown" SnapsToDevicePixels="True">
                                    <Border.Effect>
                                        <DropShadowEffect Color="Black" BlurRadius="2" ShadowDepth="0" Opacity="0.5"/>
                                    </Border.Effect>
                                    <ScrollViewer Margin="4,6,4,6" MaxHeight="{TemplateBinding MaxDropDownHeight}" SnapsToDevicePixels="True" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" CanContentScroll="True">
                                        <!-- StackPanel 用于显示子级，方法是将 IsItemsHost 设置为 True -->
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" Background="White"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Check box template -->
        <DataTemplate x:Key="CheckBoxTemplate">
            <CheckBox x:Name="InstallCheckbox" Style="{DynamicResource CheckButtonStyle}" IsChecked="{Binding IsChecked}"/>
        </DataTemplate>
        
        <!-- Combo box template -->
        <DataTemplate x:Key="ComboBoxTemplate">
            <ComboBox Name="SecurityComboBox" 
                      VerticalContentAlignment="Center"
                        Height="Auto"
                        Width="Auto"
                        Opacity="1" 
                        BorderThickness="1"
                      Cursor="Hand"
                      SelectedValuePath="Tag" 
                      SelectedValue="{Binding DictionaryValue, Mode=TwoWay}">
                <ComboBoxItem Content="None" Tag="0"/>
                <ComboBoxItem Content="WPA" Tag="1"/>
                <ComboBoxItem Content="WPA2" Tag="2"/>
            </ComboBox>
        </DataTemplate>
        
        <!-- Input box template -->        
        <DataTemplate x:Key="InputBoxTemplate">
            <Border Grid.Column="1" BorderBrush="LightGray" BorderThickness="1" CornerRadius="5" MouseLeftButtonUp="SelectedInputBox_Clicked">
                <TextBox Text="{Binding DictionaryValue}" Padding="5,0" VerticalContentAlignment="Center" Background="Transparent" BorderThickness="0" TextChanged="InputBox_TextChanged"/>
            </Border>
        </DataTemplate>

        <vm:InputTypeTemplateSelector x:Key="InputTypeTemplateSelector"
            CheckBoxTemplate="{StaticResource CheckBoxTemplate}"
            ComboBoxTemplate="{StaticResource ComboBoxTemplate}"
            InputBoxTemplate="{StaticResource InputBoxTemplate}"
        />
    </Page.Resources>
    
    <Grid>
         <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4.5*"/>
            <ColumnDefinition Width="5.5*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" Margin="0" Background="Transparent">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="1" Orientation="Vertical" VerticalAlignment="Center">
                
                <!-- Connection Mode Toggle Button -->
                <Border Style="{DynamicResource ButtonBorderStyle}" Visibility="Collapsed">
                    <Grid>
                        <Border x:Name="SlideHighlight" Background="#0078D3" Width="70" Height="35" CornerRadius="17.5" HorizontalAlignment="Left" VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <TranslateTransform x:Name="HighlightTransform" />
                            </Border.RenderTransform>
                        </Border>
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="LibUSB" Background="Transparent" Width="70"  MouseLeftButtonUp="SelectLibUSB_Clicked">
                                <TextBlock Foreground="{Binding LibUsbForeground}" Text="LibUSB" VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="tahoma" FontWeight="Bold"/>
                            </Border>
                            <Border x:Name="FT4222h" Background="Transparent" Width="70" MouseLeftButtonUp="SelectFt4222h_Clicked">
                                <TextBlock Foreground="{Binding Ft4222hForeground}" Text="FT4222h" VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="tahoma" FontWeight="Bold"/>
                            </Border>
                        </StackPanel>
                    </Grid>
                </Border>
                <!-- Connect button -->
                <Border Style="{DynamicResource ButtonBorderStyle}" MouseLeftButtonUp="ConnectButton_Clicked" >
                    <TextBlock Text="CONNECT / READ" Style="{DynamicResource ButtonTextBlockStyle}"/>
                </Border>
                <!-- Write button -->
                <Border Style="{DynamicResource ButtonBorderStyle}" MouseLeftButtonUp="WriteButton_Clicked">
                    <TextBlock Text="WRITE" Style="{DynamicResource ButtonTextBlockStyle}"/>
                </Border>
                <!-- Save template -->
                <Border Style="{DynamicResource ButtonBorderStyle}" MouseLeftButtonUp="SaveButton_Clicked" >
                    <TextBlock Text="SAVE TEMPLATE" Style="{DynamicResource ButtonTextBlockStyle}"/>
                </Border>
            </StackPanel>
            <Border Grid.Row="0" Background="Beige" BorderBrush="Black" BorderThickness="0.5" CornerRadius="15" Margin="50,0" Padding="10,10,10,5">
                <StackPanel Orientation="Vertical" VerticalAlignment="Top">
                    <Grid x:Name="Wifi_And_Password">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="SSID" FontWeight="Bold" FontFamily="roboto"/>
                        <Grid Grid.Column="1" >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="9*"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Password" FontWeight="Bold" FontFamily="roboto"/>
                            <Border Grid.Column="1" Cursor="Hand" MouseLeftButtonUp="RefreshWifiAndIpv4List_Clicked" Padding="1">
                                <Image Source="/Views/Assets/Icon_Refresh.png"/>
                            </Border>
                        </Grid>
                    </Grid>
                    <Separator BorderThickness="1" BorderBrush="Black" Margin="0,5" Opacity="0.5"/>                    
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <TextBlock x:Name="WifiNameTextBlock" Text="{Binding ConnectedWifiName}" FontFamily="roboto" VerticalAlignment="Center"/>
                            <Border Height="{Binding ActualHeight, ElementName=WifiNameTextBlock}" Margin="3,0,0,0" Opacity="0.3" Padding="2,0,0,2" MouseLeftButtonUp="CopyWifiName_Clicked">
                                <Image Cursor="Hand" Source="/Views/Assets/Icon_Copy.png" VerticalAlignment="Center"/>
                            </Border>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <TextBlock x:Name="WifiPasswordTextBlock" Text="{Binding ConnectedWifiPassword}" FontFamily="roboto" VerticalAlignment="Center"/>
                            <Border Height="{Binding ActualHeight, ElementName=WifiPasswordTextBlock}" Margin="3,0,0,0" Opacity="0.3" Padding="2,0,0,2" MouseLeftButtonUp="CopyWifiPassword_Clicked">
                                <Image Cursor="Hand" Source="/Views/Assets/Icon_Copy.png" VerticalAlignment="Center"/>
                            </Border>
                        </StackPanel>
                    </Grid>                           
                    <Grid x:Name="InterfaceName_And_Ipv4Address">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Interface Name" FontWeight="Bold" FontFamily="roboto"/>
                        <TextBlock Grid.Column="1" Text="IPv4 Address" FontWeight="Bold" FontFamily="roboto"/>
                    </Grid>
                    <Separator BorderThickness="1" BorderBrush="Black" Margin="0,5" Opacity="0.5"/>
                    <ItemsControl ItemsSource="{Binding IpAddressList}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding DictionaryKey}" FontFamily="roboto"/>
                                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                                        <TextBlock x:Name="IpAddressTextBlock" Text="{Binding DictionaryValue}" FontFamily="roboto" VerticalAlignment="Center"/>
                                        <Border Height="{Binding ActualHeight, ElementName=IpAddressTextBlock}" Margin="3,0,0,0" Opacity="0.3" Padding="2,0,0,2" VerticalAlignment="Center" MouseLeftButtonUp="CopyIPv4Address_Clicked">
                                            <Image Cursor="Hand" Source="/Views/Assets/Icon_Copy.png" VerticalAlignment="Center"/>
                                        </Border>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
            <!-- Control button -->
            <Grid Grid.Row="2" Margin="15,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0">
                    <Border Style="{DynamicResource BackButtonStyle}" MouseLeftButtonUp="BackToPrevious_Configuration_Clicked">
                        <Border.Triggers>
                            <EventTrigger RoutedEvent="Border.MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="BackLabel"
                 Storyboard.TargetProperty="(Label.Foreground).(SolidColorBrush.Color)"
                 To="Black" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="Border.MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="BackLabel"
                 Storyboard.TargetProperty="(Label.Foreground).(SolidColorBrush.Color)"
                 To="Gray" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Border.Triggers>
                        <Label x:Name="BackLabel"
                FontSize="18"
                Content="Back"
                Foreground="Gray"
                VerticalContentAlignment="Center"
                HorizontalContentAlignment="Center"/>
                    </Border>
                </Grid>
                <Grid Grid.Column="2">

                </Grid>
            </Grid>
        </Grid>
        <!-- Main Content -->
        <Grid Grid.Column="1" VerticalAlignment="Top" Background="Transparent" HorizontalAlignment="Left">
            <Border Width="400" BorderBrush="Black" BorderThickness="0.3" CornerRadius="20" Padding="15,10">
                <ItemsControl ItemsSource="{Binding WifiSettingsList}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical">
                                <Border Height="20">
                                    <TextBlock VerticalAlignment="Center" Text="{Binding SettingTitle}" FontWeight="Bold" FontFamily="tahoma"/>
                                </Border>
                                <Separator BorderThickness="1" BorderBrush="Black" Opacity="0.5" Margin="0,5,0,5"/>
                                <ItemsControl ItemsSource="{Binding SettingList}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Height="30" Margin="0,0,0,5">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="{Binding DictionaryKey}" VerticalAlignment="Center" FontFamily="arial"/>
                                                <ContentControl Grid.Column="1" 
                                                Content="{Binding}" 
                                                ContentTemplateSelector="{StaticResource InputTypeTemplateSelector}"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>                                
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Border>
        </Grid>
        
    </Grid>
</Page>
